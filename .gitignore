.DS_Store

# SSH: track only the config, never keys/known_hosts.
# The parent-dir form `.ssh/` would make the negation below unreliable.
home/.ssh/*
!home/.ssh/config

# matplotlib regenerates this cache; it's not config.
home/.matplotlib/
